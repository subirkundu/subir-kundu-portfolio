import { AtSign, Code2, Mail } from "lucide-react";
import { profile } from "@/content/profile";
export function SocialLinks({ compact = false }: { compact?: boolean }) { const links = [{ href: profile.email ? `mailto:${profile.email}` : "", label: "Email", icon: Mail }, { href: profile.linkedin, label: "LinkedIn", icon: AtSign }, { href: profile.github, label: "GitHub", icon: Code2 }].filter(link => link.href); return links.length ? <div className={compact ? "socials compact" : "socials"}>{links.map(({ href, label, icon: Icon }) => <a key={label} href={href} target={label === "Email" ? undefined : "_blank"} rel="noreferrer"><Icon aria-hidden="true" size={18}/><span>{label}</span></a>)}</div> : <p className="section-description">Direct contact details will be added when verified.</p>; }
